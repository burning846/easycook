# EasyCook 部署配置文件

# 域名配置
DOMAIN="burning233.top"
API_SUBDOMAIN="api"

# 目录配置
FRONTEND_DIR="$(pwd)/frontend"
BACKEND_DIR="$(pwd)/backend"

# 数据库配置
# 生产环境建议使用PostgreSQL或MySQL
DATABASE_URL="sqlite:///$(pwd)/backend/app.db"
# 如果使用PostgreSQL，取消下面的注释并修改配置
# DATABASE_URL="postgresql://username:password@localhost/easycook"

# 安全配置
# 生产环境必须修改为强密钥
SECRET_KEY="change-this-to-a-secure-random-string"

# Google OAuth配置
# 请在Google Cloud Console创建OAuth凭据
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"